# Linux 基于 DBus 的多线程通信系统

## 第一部分：C语言 Socket 编程

### 功能
1. TCP 协议实现客户端和服务器端
2. 服务器端支持多线程处理多个客户端
3. 客户端从 console 输入数据，服务器接收并打印
### 编译
```bash
make
```

### 运行
```bash
# 终端1: 启动服务器
./server

# 终端2: 启动客户端
./client
```

### 测试
在客户端终端输入消息，服务器会接收并打印，然后返回响应。
输入 `quit` 退出客户端。
